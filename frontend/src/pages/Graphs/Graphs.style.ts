import styled from 'styled-components';
import { Doughnut } from 'react-chartjs-2';

export const StyledGraphs = styled.div`
  display: flex;
  flex: 1 0 auto;
  flex-direction: column;
`;

export const PieChart = styled(Doughnut)``;
